apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: airex-free

resources:
  - ../../base
  - namespace.yaml
  - postgres.yaml
  - redis.yaml
  - secrets.yaml

patches:
  - path: configmap.yaml
  - path: ingress.yaml
