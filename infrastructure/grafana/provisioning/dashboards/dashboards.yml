apiVersion: 1

providers:
  - name: "AIREX"
    orgId: 1
    folder: "AIREX"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
